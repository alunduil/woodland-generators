coverage:
  status:
    project:
      default:
        target: 50%
        threshold: 5%
        if_not_found: success
    patch:
      default:
        target: 60%
        threshold: 5%
        if_not_found: success

comment:
  layout: "reach,diff,flags,tree"
  behavior: default
  require_changes: false

ignore:
  - "dist/"
  - "coverage/"
  - "benchmarks/"
  - "tools/"
  - "test/"
  - "**/*.d.ts"
  - "src/cli.ts"
